#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	int c=4;
	double x=0.5;
	double pi=3.14159;
	double a,b;
	
	a=sqrt(pow(sin(x),2.5));
	b=pow(c,x*x)/sqrt(2*pi);
	cout<<a<<endl;
	cout<<b<<endl;
	
	return 0;
}

